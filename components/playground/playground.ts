'use client';

// Export the new main component as default
export { default as Playground } from './index';

// We can export sub-components if needed
export { CodeEditorWrapper } from './code-editor-wrapper';
export { OutputPanel } from './output-panel';
export { PlaygroundNav } from './playground-nav';
export { StatusBar } from './status-bar';
